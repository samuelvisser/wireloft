# Template for DailyWire downloads; keep as a separate file under /etc/cron.d/
# The placeholder {{schedule}} will be replaced at container start.

{{schedule}} root dailywire-downloader >> /var/log/cron.log 2>&1
